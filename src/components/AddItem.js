import React from "react";

const AddItem = props => {
  return (
    <form className="add-item-container">
      <input type="text" />
      <button>&#10010;</button>
    </form>
  );
};

export default AddItem;
